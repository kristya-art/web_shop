<?php
//
//$users = {
//    'bob' => '123';
//};

/**
 * on this page we will save the user, so that we take the users name and the users password , than we save it
 */

/**
 * than we have to check if this session has an user with if(!isset($_SESSION[$user])
 */

/*
 * the 3d method will denied access if the user is not 'user'
 */


//but better way is to create one file"authentication" and include it in each file we need